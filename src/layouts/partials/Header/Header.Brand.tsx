import styles from './header.module.scss'
// import { useTheme } from '@/contexts/ThemeProvider'

const HeaderBrand = () => {
  // const { darkTheme } = useTheme()
  
  return (
    <div className={styles.Brand}>SampleReact</div>
  )
}

export default HeaderBrand